[project]
name = "grammar-based-input-generation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "hypothesis>=6.152.8",
    "pytest>=9.0.3",
    "pytest-html>=4.2.0",
]
